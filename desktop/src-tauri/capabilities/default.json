{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window can call only application-owned local AI commands.",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
